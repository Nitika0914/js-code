"use strict" //treat all js code as newer version

// alert(3+3) //we are using node.js and not browser

let name = "Nitika";
let age = 20;
let isLoggedIn = true

// number => 2 to power 53
// bigint => used for very large numbers
// string => ""
// boolean => true/false
// null => standalone value (there is no value)
// undefined => when variable is declared but no value if given
// symbol => used for uniqueness

// object

console.log(typeof "Nitika");
console.log(typeof null); //null is an object
console.log(typeof undefined);